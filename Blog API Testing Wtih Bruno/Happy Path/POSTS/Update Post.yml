info:
  name: Update Post
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/api/posts/69f2f6807b4510af72aa0cf2"
  body:
    type: json
    data: |-
      {
        "title": "Post Updated Testing",
        "content": "I have just updated my post for testing  purposes."
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
