info:
  name: Delete Post
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/posts/69f2f6807b4510af72aa0cf2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
