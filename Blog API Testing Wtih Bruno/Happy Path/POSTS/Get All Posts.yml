info:
  name: Get All Posts
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/posts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
